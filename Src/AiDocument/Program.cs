namespace AiDocument;

internal class Program
{
    /// <summary>
    /// The entry point of the application.
    /// </summary>
    /// <param name="args">An array of command-line arguments passed to the application.</param>
    /// <remarks>
    /// This method serves as the starting point for the program execution.
    /// It prints "Hello, World!" to the console, which is a common introductory example
    /// for demonstrating the basic syntax of a programming language.
    /// The <paramref name="args"/> parameter allows for the passing of command-line arguments,
    /// although in this specific implementation, they are not utilized.
    /// </remarks>
    static void Main(string[] args)
    {
        Console.WriteLine("Hello, World!");
    }
}
